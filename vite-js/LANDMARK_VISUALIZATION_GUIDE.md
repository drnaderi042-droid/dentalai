# 📊 راهنمای Visualization لندمارک‌ها

## ✨ ویژگی جدید: نمایش بصری Landmarks

بعد از تشخیص لندمارک‌ها توسط AI، حالا می‌توانید آن‌ها را به صورت بصری روی تصویر ببینید!

---

## 🎯 امکانات

### 1️⃣ نمایش تعاملی
- ✅ **نقاط رنگی**: هر نوع landmark با رنگ مخصوص
  - 🔵 **آبی**: نقاط اسکلتی (S, N, A, B, Pog, Go, Me, ...)
  - 🔴 **قرمز**: نقاط دندانی (U1, L1, U1A, L1A)
- ✅ **برچسب‌ها**: نام هر landmark کنار آن نمایش داده می‌شود
- ✅ **Confidence**: درصد اطمینان مدل (اگر موجود باشد)

### 2️⃣ خطوط ارتباطی
خطوط بین landmarks مهم برای نمایش:
- خط **SNA** (S-N-A)
- خط **SNB** (S-N-B)
- صفحه **فرانکفورت** (Or-Po)
- صفحه **ماندیبولار** (Go-Me)
- صفحه **اکلوژن** (ANS-PNS)
- **Facial profile** (N-Pog)
- ارتباط **دندانی** (U1-L1)

### 3️⃣ کنترل‌های تعاملی

#### 🔍 Zoom & Pan
- **Zoom In**: بزرگنمایی تصویر
- **Zoom Out**: کوچک کردن تصویر
- **Reset**: بازگشت به حالت اولیه
- **Mouse Wheel**: zoom با scroll
- **Click & Drag**: جابجایی تصویر

#### ⚙️ تنظیمات
- **برچسب‌ها**: نمایش/مخفی کردن نام landmarks
- **خطوط**: نمایش/مخفی کردن خطوط ارتباطی
- **اندازه نقاط**: تنظیم اندازه نقاط landmarks (4-16 پیکسل)

---

## 📖 نحوه استفاده

### مرحله 1: تست مدل
1. به صفحه تست بروید: `/dashboard/ai-model-test`
2. تصویر آپلود کنید
3. مدل را انتخاب کنید
4. تست را اجرا کنید

### مرحله 2: مشاهده نتایج
بعد از دریافت نتیجه موفق:
- 📊 **قسمت "نمایش بصری Landmarks"** خودکار ظاهر می‌شود
- تصویر با landmarks روی آن نمایش داده می‌شود

### مرحله 3: تعامل با Visualization
```
🔍 Zoom:
  - دکمه + / - برای zoom
  - Scroll mouse برای zoom
  - دکمه Reset برای بازگشت

👆 Pan:
  - کلیک و بکشید برای جابجایی
  - در موبایل: لمس و بکشید

⚙️ تنظیمات:
  - برچسب‌ها: روشن/خاموش
  - خطوط: روشن/خاموش
  - اندازه: slider 4-16
```

---

## 🎨 رنگ‌بندی Landmarks

### نقاط اسکلتی (آبی - #2196F3)
```
S   - Sella (مرکز سلا تورسیکا)
N   - Nasion (قدامی‌ترین نقطه درز فرونتونازال)
A   - Point A (عمیق‌ترین نقطه ماگزیلا)
B   - Point B (عمیق‌ترین نقطه ماندیبل)
Pog - Pogonion (قدامی‌ترین نقطه چانه)
Go  - Gonion (زاویه ماندیبل)
Me  - Menton (پایین‌ترین نقطه سمفیز)
Or  - Orbitale (پایین‌ترین نقطه اربیت)
Po  - Porion (بالایی‌ترین نقطه مجرای شنوایی)
ANS - Anterior Nasal Spine
PNS - Posterior Nasal Spine
```

### نقاط دندانی (قرمز - #F44336)
```
U1  - Upper Incisor (لبه برشی دندان جلوی بالا)
L1  - Lower Incisor (لبه برشی دندان جلوی پایین)
U1A - Upper Incisor Apex (راس ریشه)
L1A - Lower Incisor Apex (راس ریشه)
```

---

## 💻 برای توسعه‌دهندگان

### استفاده از کامپوننت

```jsx
import { LandmarkVisualizer } from 'src/components/landmark-visualizer';

<LandmarkVisualizer
  imageUrl="/path/to/image.jpg"
  landmarks={{
    S: { x: 903, y: 647, confidence: 0.95 },
    N: { x: 998, y: 523, confidence: 0.88 },
    // ...
  }}
  imageSize={{ width: 1200, height: 1000 }}
  showLabels={true}
/>
```

### Props

| Prop | Type | Default | توضیح |
|------|------|---------|-------|
| `imageUrl` | string | - | URL تصویر (required) |
| `landmarks` | object | - | مختصات landmarks (required) |
| `imageSize` | object | - | ابعاد تصویر `{width, height}` |
| `showLabels` | boolean | true | نمایش برچسب‌ها |

### ساختار landmarks

```typescript
{
  [landmarkName: string]: {
    x: number;           // مختصات X
    y: number;           // مختصات Y
    confidence?: number; // اطمینان (0-1) - اختیاری
  }
}
```

---

## 🎓 اصول طراحی

### 1. Performance
- ✅ استفاده از Canvas برای رندر سریع
- ✅ Debounce برای resize events
- ✅ Lazy loading تصویر

### 2. User Experience
- ✅ Responsive design
- ✅ Touch support برای موبایل
- ✅ Keyboard shortcuts (آینده)
- ✅ Loading states

### 3. Accessibility
- ✅ High contrast colors
- ✅ Clear labels
- ✅ Tooltips (آینده)

---

## 🔧 تنظیمات پیشرفته

### تغییر رنگ‌ها

در `landmark-visualizer.jsx`:

```javascript
const LANDMARK_COLORS = {
  S: '#2196F3',    // آبی
  N: '#2196F3',
  // ...
  U1: '#F44336',   // قرمز
  // ...
  default: '#4CAF50',  // سبز (برای سایر نقاط)
};
```

### اضافه کردن خطوط جدید

```javascript
const LANDMARK_LINES = [
  ['S', 'N'],        // خط بین S و N
  ['N', 'A'],        // خط بین N و A
  // خط جدید خود را اضافه کنید:
  ['YourPoint1', 'YourPoint2'],
];
```

### تغییر استایل خطوط

```javascript
// در تابع drawCanvas
ctx.strokeStyle = 'rgba(255, 255, 255, 0.5)';  // رنگ
ctx.lineWidth = 2;                              // ضخامت
ctx.setLineDash([5, 5]);                        // نقطه‌چین
```

---

## 📊 مثال کامل

```jsx
import { useState } from 'react';
import { LandmarkVisualizer } from 'src/components/landmark-visualizer';

export default function MyPage() {
  const [landmarks, setLandmarks] = useState(null);

  const handleDetection = async () => {
    // فرض: دریافت از API
    const result = await detectLandmarks(imageFile);
    setLandmarks(result.landmarks);
  };

  return (
    <div>
      <button onClick={handleDetection}>تشخیص</button>
      
      {landmarks && (
        <LandmarkVisualizer
          imageUrl={imagePreview}
          landmarks={landmarks}
          imageSize={{ width: 1200, height: 1000 }}
        />
      )}
    </div>
  );
}
```

---

## 🐛 مشکلات رایج

### تصویر نمایش داده نمی‌شود

**علت**: URL تصویر نامعتبر یا CORS
```javascript
// راه‌حل: اطمینان از URL صحیح
const img = new Image();
img.crossOrigin = 'anonymous';  // برای CORS
img.src = imageUrl;
```

### لندمارک‌ها در جای اشتباه

**علت**: Scaling نادرست
```javascript
// مطمئن شوید coordinates با ابعاد تصویر مطابقت دارند
// اگر scaled نیستند، از auto-scaling استفاده کنید
```

### Performance پایین

**راه‌حل**:
- تعداد landmarks را کم کنید
- از تصاویر کوچک‌تر استفاده کنید
- Canvas size را محدود کنید

---

## 🎯 Features آینده

- [ ] **Export تصویر**: ذخیره تصویر با landmarks
- [ ] **Measurements**: نمایش فواصل و زوایا
- [ ] **Comparison**: مقایسه چند نتیجه
- [ ] **Animation**: انیمیشن برای تغییرات
- [ ] **3D View**: نمایش سه‌بعدی (اختیاری)
- [ ] **Edit Mode**: ویرایش دستی landmarks
- [ ] **Undo/Redo**: بازگشت تغییرات

---

## 📚 منابع مرتبط

- [راهنمای تست مدل‌ها](AI_MODEL_TEST_GUIDE.md)
- [راهنمای Scaling](SCALING_CORRECTION_GUIDE.md)
- [مستندات کامل](AI_MODEL_TESTING_COMPLETE.md)

---

## 💡 نکات مفید

1. **کیفیت تصویر**: تصاویر با کیفیت بهتر نمایش بهتری دارند
2. **Zoom**: برای دقت بیشتر zoom کنید
3. **Labels**: اگر شلوغ است، برچسب‌ها را خاموش کنید
4. **Screenshot**: با Print Screen می‌توانید عکس بگیرید
5. **Browser DevTools**: با F12 console را برای debug باز کنید

---

## 🎨 اسکرین‌شات‌ها

### نمایش کامل
```
[تصویر با تمام landmarks، خطوط و برچسب‌ها]
```

### بدون خطوط
```
[فقط نقاط و برچسب‌ها]
```

### Zoomed In
```
[نمایش دقیق یک ناحیه]
```

---

## ✅ چک‌لیست

- [x] نمایش تصویر
- [x] رسم landmarks
- [x] رسم خطوط
- [x] نمایش برچسب‌ها
- [x] Zoom & Pan
- [x] کنترل‌های تعاملی
- [x] Responsive design
- [x] Touch support
- [x] رنگ‌بندی بر اساس نوع
- [x] Legend (راهنما)
- [x] نمایش confidence

---

**نسخه**: 1.0.0  
**تاریخ**: 2025-10-30  
**وضعیت**: ✅ آماده استفاده

**ساخته شده با ❤️ برای DentalAI**

---

**سوال دارید؟**
- 📖 [مستندات کامل](AI_MODEL_TESTING_COMPLETE.md)
- 💬 تیم پشتیبانی

**موفق باشید! 🎉**

