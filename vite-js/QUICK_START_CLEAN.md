# 🧹 راهنمای سریع: حذف کدهای استفاده نشده

## ⚡ استفاده سریع

### 1. حذف Import های استفاده نشده (توصیه می‌شود)

```bash
npm run clean:unused:imports
```

این دستور به صورت خودکار همه import های استفاده نشده را پیدا و حذف می‌کند.

### 2. بررسی جامع همه کدهای استفاده نشده

```bash
npm run clean:unused
```

این دستور موارد زیر را بررسی می‌کند:
- ✅ Import های استفاده نشده
- ✅ فایل‌های استفاده نشده  
- ✅ Dependencies استفاده نشده

### 3. بررسی Dependencies استفاده نشده

```bash
npm run clean:unused:deps
```

### 4. حذف همه موارد استفاده نشده

```bash
npm run clean:all
```

---

## 📋 دستورات موجود

| دستور | توضیحات |
|-------|---------|
| `npm run clean:unused:imports` | حذف خودکار import های استفاده نشده |
| `npm run clean:unused` | بررسی جامع همه موارد استفاده نشده |
| `npm run clean:unused:deps` | بررسی dependencies استفاده نشده |
| `npm run clean:all` | حذف همه موارد استفاده نشده |

---

## 💡 نکات

1. **قبل از حذف**: همیشه کدها را commit کنید
2. **بعد از حذف**: تست‌ها را اجرا کنید
3. **مرحله به مرحله**: کدها را تدریجی حذف کنید

---

برای اطلاعات بیشتر، فایل `CLEAN_CODE_GUIDE.md` را مطالعه کنید.










