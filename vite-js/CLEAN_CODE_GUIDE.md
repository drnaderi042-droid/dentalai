# راهنمای پیدا کردن و حذف کدهای استفاده نشده

این راهنما روش‌های مختلف برای پیدا کردن و حذف کدهای استفاده نشده در پروژه را توضیح می‌دهد.

## 📋 فهرست مطالب

1. [Import های استفاده نشده](#1-import-های-استفاده-نشده)
2. [فایل‌های استفاده نشده](#2-فایل‌های-استفاده-نشده)
3. [Dependencies استفاده نشده](#3-dependencies-استفاده-نشده)
4. [تحلیل Bundle Size](#4-تحلیل-bundle-size)
5. [اسکریپت‌های خودکار](#5-اسکریپت‌های-خودکار)

---

## 1. Import های استفاده نشده

### روش 1: استفاده از ESLint (توصیه می‌شود)

پروژه شما از `eslint-plugin-unused-imports` استفاده می‌کند که به صورت خودکار import های استفاده نشده را پیدا می‌کند.

#### اجرای دستی:

```bash
# بررسی import های استفاده نشده
npm run lint

# حذف خودکار import های استفاده نشده
npm run clean:unused:imports
```

#### تنظیمات ESLint

در فایل `.eslintrc.cjs`، rule زیر فعال است:

```javascript
'unused-imports/no-unused-imports': 1, // warning
```

برای تبدیل به error (خطا):

```javascript
'unused-imports/no-unused-imports': 2, // error
```

### روش 2: استفاده از اسکریپت جامع

```bash
npm run clean:unused
```

این اسکریپت همه موارد استفاده نشده را بررسی می‌کند.

---

## 2. فایل‌های استفاده نشده

### استفاده از اسکریپت جامع

```bash
npm run clean:unused
```

این اسکریپت فایل‌هایی که در هیچ جای پروژه import نشده‌اند را پیدا می‌کند.

⚠️ **توجه**: قبل از حذف فایل‌ها، حتماً دستی بررسی کنید. ممکن است برخی فایل‌ها به صورت dynamic import یا در runtime استفاده شوند.

### فایل‌های استثنا

اسکریپت به صورت خودکار فایل‌های زیر را نادیده می‌گیرد:
- فایل‌های `_mock`
- فایل‌های `index.js`
- فایل‌های `main.jsx` و `app.jsx`
- فایل‌های تست (`.test.js`, `.spec.js`)

---

## 3. Dependencies استفاده نشده

### نصب depcheck

```bash
npm install -g depcheck
# یا
npm install --save-dev depcheck
```

### اجرای depcheck

```bash
# استفاده از npx (بدون نصب)
npx depcheck

# یا استفاده از اسکریپت
npm run clean:unused:deps
```

### خروجی نمونه

```
Unused dependencies
* package-name-1
* package-name-2

Unused devDependencies
* dev-package-name-1
```

⚠️ **توجه**: 
- برخی packages ممکن است به صورت global یا در config files استفاده شوند
- قبل از حذف، حتماً بررسی کنید

---

## 4. تحلیل Bundle Size

### نصب rollup-plugin-visualizer

```bash
npm install --save-dev rollup-plugin-visualizer
```

### اضافه کردن به vite.config.js

```javascript
import { visualizer } from 'rollup-plugin-visualizer';

export default defineConfig({
  plugins: [
    // ... سایر plugins
    visualizer({
      open: true,
      gzipSize: true,
      brotliSize: true,
      filename: 'dist/stats.html',
    }),
  ],
});
```

### اجرا

```bash
npm run analyze:bundle
```

این دستور یک فایل HTML با تجسم bundle size ایجاد می‌کند.

---

## 5. اسکریپت‌های خودکار

### اسکریپت جامع

```bash
npm run clean:unused
```

این اسکریپت موارد زیر را بررسی می‌کند:
- ✅ Import های استفاده نشده (با ESLint)
- ✅ فایل‌های استفاده نشده
- ✅ Dependencies استفاده نشده (با depcheck)

### خروجی نمونه

```
🔍 شروع بررسی کدهای استفاده نشده...
============================================================

📦 بررسی Import های استفاده نشده...
⚠️  5 فایل با Import های استفاده نشده پیدا شد:
  src/components/example.jsx:
    Line 5: 'unusedImport' is defined but never used

📁 بررسی فایل‌های استفاده نشده...
⚠️  3 فایل احتمالاً استفاده نشده پیدا شد:
  src/components/old-component.jsx

📚 بررسی Dependencies استفاده نشده...
⚠️  2 Dependency استفاده نشده پیدا شد:
  old-package

============================================================
📊 خلاصه گزارش
============================================================
```

---

## 🛠️ تنظیمات پیشرفته

### فعال‌سازی Auto-fix در ESLint

در VS Code، می‌توانید تنظیمات زیر را اضافه کنید (`.vscode/settings.json`):

```json
{
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "eslint.validate": [
    "javascript",
    "javascriptreact",
    "typescript",
    "typescriptreact"
  ]
}
```

### استفاده از Git Hooks

برای بررسی خودکار قبل از commit، می‌توانید از husky استفاده کنید:

```bash
npm install --save-dev husky lint-staged
```

سپس در `package.json`:

```json
{
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
```

---

## 📝 نکات مهم

1. **همیشه قبل از حذف بررسی کنید**: برخی کدها ممکن است به صورت dynamic یا در runtime استفاده شوند.

2. **از Git استفاده کنید**: قبل از حذف کدها، حتماً commit کنید تا در صورت نیاز بتوانید بازگردانید.

3. **تست کنید**: بعد از حذف کدها، حتماً تست‌ها را اجرا کنید.

4. **مرحله به مرحله**: کدها را به صورت تدریجی حذف کنید، نه همه یکجا.

---

## 🔗 منابع مفید

- [ESLint Unused Imports Plugin](https://github.com/sweepline/eslint-plugin-unused-imports)
- [depcheck Documentation](https://github.com/depcheck/depcheck)
- [Vite Bundle Analyzer](https://github.com/doesdev/rollup-plugin-analyzer)

---

## ❓ سوالات متداول

### Q: چرا برخی import ها به عنوان استفاده نشده نشان داده می‌شوند؟

A: ممکن است به صورت type-only import باشند یا در JSX استفاده شوند. می‌توانید از comment استفاده کنید:
```javascript
// eslint-disable-next-line unused-imports/no-unused-imports
import { Type } from './types';
```

### Q: چگونه می‌توانم یک فایل را از بررسی استثنا کنم؟

A: در فایل `.eslintignore` یا در بالای فایل:
```javascript
/* eslint-disable unused-imports/no-unused-imports */
```

### Q: depcheck برخی packages را اشتباه نشان می‌دهد

A: می‌توانید در فایل `package.json` یا `.depcheckrc.json` تنظیمات اضافه کنید:
```json
{
  "ignoreMatches": ["package-name"]
}
```










