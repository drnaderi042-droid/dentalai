# راهنمای تنظیم و استفاده از چت

## تنظیمات اولیه

### ۱. فایل محیطی (Environment Variables)

برای استفاده از چت، باید فایل `.env.local` را در پوشه `vite-js` ایجاد کنید:

```bash
# vite-js/.env.local

# Server URL for API calls
VITE_SERVER_URL=http://localhost:7272

# Base path for assets (optional)
VITE_BASE_PATH=

# Asset URL (optional)
VITE_ASSET_URL=
```

### ۲. اجرای Backend API

مطمئن شوید که backend API در حال اجرا است:

```bash
cd minimal-api-dev-v6
npm run dev
```

Backend باید روی پورت `7272` اجرا شود.

### ۳. اجرای Frontend

```bash
cd vite-js
npm run dev
```

## قابلیت‌های چت

### ✅ قابلیت‌های فعلی

1. **ارسال پیام متنی**: ارسال پیام‌های متنی بین دکترها
2. **ارسال تصویر**: آپلود و ارسال تصویر در چت
3. **نمایش تصویر**: مشاهده تصاویر ارسال شده
4. **لیست مکالمات**: نمایش لیست تمام چت‌ها
5. **جستجو در مکالمات**: جستجوی سریع در چت‌ها
6. **خواندن پیام‌ها**: علامت‌گذاری خودکار پیام‌ها به عنوان خوانده شده
7. **Hover Actions**: دکمه‌های Reply, React, Delete با hover روی پیام

### 🔄 تغییرات اخیر

#### Backend:
- ✅ اضافه شدن فیلد `attachments` و `contentType` به `Message` model
- ✅ پشتیبانی از آپلود تصویر در API `/api/upload/chat`
- ✅ پشتیبانی از ارسال attachments در API `/api/chat/send`
- ✅ Parse کردن attachments در API `/api/chat/list` و `/api/chat/[chatId]`

#### Frontend:
- ✅ تغییر از context menu به hover actions (بهبود UX)
- ✅ اضافه شدن `EmptyContent` برای حالت بدون انتخاب چت
- ✅ پشتیبانی کامل از آپلود و نمایش تصویر
- ✅ بهبود UI برای نمایش پیام‌های تصویری

## نحوه استفاده

### ارسال پیام متنی
1. یک مکالمه را انتخاب کنید یا مکالمه جدیدی شروع کنید
2. پیام خود را در کادر ورودی تایپ کنید
3. کلید Enter را فشار دهید یا روی دکمه ارسال کلیک کنید

### ارسال تصویر
1. روی آیکن گالری (📷) کلیک کنید
2. تصویر مورد نظر را انتخاب کنید
3. منتظر بمانید تا آپلود تکمیل شود
4. پیام با تصویر ارسال می‌شود

### Actions روی پیام
1. ماوس را روی پیام ببرید (hover)
2. دکمه‌های Reply, React, Delete نمایش داده می‌شوند
3. روی دکمه مورد نظر کلیک کنید

## مشکلات رایج و راه‌حل‌ها

### ❌ خطای "Authorization token missing"
- مطمئن شوید که لاگین کرده‌اید
- توکن شما منقضی شده است، دوباره لاگین کنید

### ❌ تصویر آپلود نمی‌شود
- مطمئن شوید Backend در حال اجرا است
- چک کنید که `VITE_SERVER_URL` در `.env.local` درست تنظیم شده است
- بررسی کنید که حجم تصویر کمتر از 10MB باشد

### ❌ پیام ارسال نمی‌شود
- مطمئن شوید recipient (گیرنده) انتخاب شده است
- چک کنید که هر دو طرف چت دکتر هستند (فقط دکترها می‌توانند چت کنند)

### ❌ چت خالی است
- اگر هیچ چتی ندارید، روی دکمه compose کلیک کنید و یک دکتر را انتخاب کنید
- اگر EmptyContent نمایش داده می‌شود، یک چت را از لیست انتخاب کنید

## API Endpoints

### Chat APIs:
- `GET /api/chat/list` - دریافت لیست تمام چت‌ها
- `GET /api/chat/[chatId]` - دریافت جزئیات یک چت خاص
- `POST /api/chat/send` - ارسال پیام جدید
- `GET /api/chat/doctors` - دریافت لیست دکترها

### Upload API:
- `POST /api/upload/chat` - آپلود فایل/تصویر برای چت

## توسعه آینده

### 🚀 قابلیت‌های برنامه‌ریزی شده:
- [ ] React به پیام‌ها با emoji
- [ ] Reply به پیام‌ها
- [ ] حذف پیام‌ها
- [ ] ویرایش پیام‌ها
- [ ] ارسال فایل (غیر از تصویر)
- [ ] نمایش وضعیت آنلاین/آفلاین
- [ ] Typing indicator
- [ ] پشتیبانی از پیام‌های صوتی
- [ ] چت گروهی
- [ ] نوتیفیکیشن real-time با WebSocket

## پشتیبانی

اگر مشکلی دارید، لطفاً در repository مربوطه issue ایجاد کنید.

