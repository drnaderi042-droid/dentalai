# 🔧 راهنمای تصحیح مقیاس (Scaling Correction)

## 🎯 مشکل

وقتی مدل‌های AI تصاویر را تحلیل می‌کنند، گاهی مختصات لندمارک‌ها با ابعاد واقعی تصویر متفاوت است. این به دلیل:

1. **Scaling داخلی مدل**: مدل تصویر را به اندازه استاندارد تبدیل می‌کند
2. **Compression**: برای کاهش حجم، تصویر فشرده می‌شود
3. **نرمال‌سازی**: بعضی مدل‌ها مختصات را نرمال می‌کنند

### مثال:

```json
// نتیجه مدل (نادرست - مقیاس کوچک)
{
  "S": {"x": 342, "y": 245},
  "N": {"x": 378, "y": 198}
}

// تصویر واقعی: 1200×1000 پیکسل
// محدوده landmarks: ~410×425
// ضریب scaling: ~2.5-3
```

---

## ✅ راه‌حل خودکار (در صفحه وب)

صفحه تست مدل‌های AI حالا **خودکار** این مشکل را حل می‌کند:

### 1️⃣ فعال‌سازی

در صفحه تست:
- ✅ گزینه **"تصحیح خودکار مقیاس"** به صورت پیش‌فرض فعال است
- می‌توانید آن را خاموش کنید اگر نمی‌خواهید

### 2️⃣ نحوه کار

```javascript
// 1. هنگام آپلود، ابعاد تصویر ذخیره می‌شود
Image size: 1200 × 1000 pixels

// 2. بعد از دریافت نتیجه، محاسبه می‌شود
Landmark range: 410 × 425
Scale X: 1200 / 410 = 2.93
Scale Y: 1000 / 425 = 2.35
Final factor: (2.93 + 2.35) / 2 = 2.64

// 3. مختصات ضرب در ضریب می‌شوند
Original: S = (342, 245)
Scaled:   S = (903, 647)
```

### 3️⃣ نمایش نتایج

بعد از تست، می‌بینید:
- 🔧 **مقیاس خودکار اعمال شد: ضریب 2.64**
- مختصات اصلاح شده در JSON نمایش داده می‌شود
- مختصات اصلی هم ذخیره می‌شود (در `original_landmarks`)

---

## 🐍 راه‌حل دستی (اسکریپت Python)

اگر می‌خواهید خارج از صفحه وب محاسبه کنید:

### استفاده:

```bash
cd vite-js

# روش 1: با فایل JSON
python calculate_scaling.py /path/to/image.jpg results.json

# روش 2: با paste کردن JSON
python calculate_scaling.py /path/to/image.jpg
# سپس JSON را paste کنید
```

### خروجی:

```
📏 ابعاد واقعی تصویر: 1200 × 1000 پیکسل
📍 محدوده landmarks: 410 × 425
   X: 285 - 410
   Y: 198 - 425

🔢 ضرایب محاسبه شده:
   روش 1 (max dimension): 2.82
   روش 2 (width based):   2.93
   روش 3 (height based):  2.35

✅ ضریب توصیه شده: 2.64

📊 نتایج Scaled:
{
  "landmarks": {
    "S": {"x": 903.28, "y": 646.80},
    "N": {"x": 997.92, "y": 522.72},
    ...
  },
  "scaling_info": {
    "factor": 2.64,
    "applied": true
  }
}

💾 نتایج در landmarks_scaled.json ذخیره شد
```

---

## 🔢 محاسبه دستی

اگر می‌خواهید خودتان محاسبه کنید:

### فرمول:

```
ضریب Scaling = ابعاد واقعی تصویر / محدوده landmarks
```

### مثال با داده‌های شما:

```python
# داده‌های شما
landmarks = {
  "S": {"x": 342, "y": 245},
  "N": {"x": 378, "y": 198},
  "A": {"x": 395, "y": 315},
  ...
}

# 1. پیدا کردن محدوده
x_coords = [342, 378, 395, 385, 388, 285, 375, 405, 315, 398, 310, 410, 398]
y_coords = [245, 198, 315, 378, 398, 420, 425, 248, 255, 298, 295, 345, 365]

max_x = 410
max_y = 425

# 2. فرض: تصویر شما 1200×1000 است
image_width = 1200
image_height = 1000

# 3. محاسبه ضریب
scale_x = image_width / max_x   # = 1200 / 410 = 2.93
scale_y = image_height / max_y  # = 1000 / 425 = 2.35

# 4. میانگین (توصیه می‌شود)
scaling_factor = (scale_x + scale_y) / 2  # = 2.64

# 5. اعمال
S_scaled = {
    "x": 342 * 2.64 = 903,
    "y": 245 * 2.64 = 647
}
```

---

## 📏 چگونه ابعاد تصویر را پیدا کنیم؟

### روش 1: Python
```python
from PIL import Image

img = Image.open("your_image.jpg")
print(img.size)  # (width, height)
```

### روش 2: ویندوز
- راست کلیک روی تصویر
- Properties → Details
- Dimensions: 1200 × 1000

### روش 3: لینوکس/مک
```bash
identify your_image.jpg
# یا
file your_image.jpg
```

### روش 4: مرورگر
```javascript
const img = new Image();
img.onload = () => {
  console.log(img.width, img.height);
};
img.src = 'path/to/image.jpg';
```

---

## ⚠️ نکات مهم

### 1. چه زمانی scaling لازم است؟

Scaling فقط وقتی لازم است که:
- ✅ ضریب > 1.5 یا < 0.5 باشد
- ✅ مختصات خیلی کوچک/بزرگ باشند
- ✅ مختصات خارج از محدوده تصویر باشند

### 2. چگونه بفهمیم scaling درست است؟

بعد از scaling:
- ✅ مختصات باید در محدوده تصویر باشند
- ✅ نسبت‌ها باید حفظ شوند
- ✅ فواصل نسبی باید صحیح باشند

### 3. خطاهای رایج

❌ **استفاده از ضریب ثابت**
```javascript
// اشتباه - هر تصویر ضریب متفاوتی دارد
landmarks.x *= 2.5  // ❌
```

✅ **محاسبه بر اساس تصویر**
```javascript
// درست
const factor = imageWidth / maxLandmarkX
landmarks.x *= factor  // ✅
```

---

## 🧪 تست صحت Scaling

### چک‌لیست:

```javascript
// 1. مختصات در محدوده تصویر
S.x < imageWidth  ✓
S.y < imageHeight ✓

// 2. لندمارک‌ها منطقی هستند
S.y < N.y  // سلا بالاتر از نازیون
A.y < B.y  // پوینت A بالاتر از B
✓

// 3. فاصله‌ها معقول هستند
distance(S, N) ~ 100-200 pixels  ✓
distance(A, B) ~ 50-100 pixels   ✓
```

### مثال تست:

```python
def verify_scaling(scaled_landmarks, image_size):
    """تست صحت scaling"""
    
    # 1. همه در محدوده تصویر باشند
    for name, lm in scaled_landmarks.items():
        assert 0 <= lm['x'] <= image_size['width'], f"{name}.x out of bounds"
        assert 0 <= lm['y'] <= image_size['height'], f"{name}.y out of bounds"
    
    # 2. ترتیب عمودی صحیح باشد
    assert scaled_landmarks['S']['y'] < scaled_landmarks['Go']['y'], "S should be above Go"
    assert scaled_landmarks['N']['y'] < scaled_landmarks['Me']['y'], "N should be above Me"
    
    # 3. فاصله‌ها معقول باشند
    sn_distance = distance(scaled_landmarks['S'], scaled_landmarks['N'])
    assert 50 < sn_distance < 300, "S-N distance unreasonable"
    
    print("✅ All scaling tests passed!")
```

---

## 💡 توصیه‌ها

### برای دقت بیشتر:

1. **از تصاویر با کیفیت بالا استفاده کنید**
   - حداقل 800×800 پیکسل
   - فرمت PNG یا JPG با کیفیت بالا

2. **scaling خودکار را فعال کنید**
   - در صفحه وب، گزینه را روشن نگه دارید
   - console را برای لاگ‌های scaling چک کنید

3. **نتایج را بررسی کنید**
   - همیشه مختصات scaled را با تصویر مقایسه کنید
   - از ابزارهای visualization استفاده کنید

4. **مختصات اصلی را نگه دارید**
   - برای reference
   - برای debug کردن

---

## 📚 منابع بیشتر

- [راهنمای تست مدل‌ها](AI_MODEL_TEST_GUIDE.md)
- [مستندات کامل](AI_MODEL_TESTING_COMPLETE.md)
- [OpenRouter Documentation](https://openrouter.ai/docs)

---

## ❓ سوالات متداول

### Q: چرا مدل‌ها scaling اعمال می‌کنند؟

A: برای کاهش حجم محاسبات و استاندارد کردن ورودی.

### Q: آیا همه مدل‌ها scaling دارند؟

A: نه، بستگی به مدل دارد. بعضی مدل‌ها ابعاد اصلی را حفظ می‌کنند.

### Q: چگونه بفهمم scaling لازم است یا نه؟

A: اگر مختصات خیلی کوچک یا خیلی بزرگ بودند، scaling لازم است.

### Q: آیا scaling دقت را کم می‌کند؟

A: نه، فقط مقیاس را تغییر می‌دهد. دقت نسبی حفظ می‌شود.

---

**نسخه**: 1.0.0  
**تاریخ**: 2025-10-30  
**به‌روزرسانی**: Auto-scaling در صفحه وب اضافه شد

**سوال دارید؟** به [مستندات کامل](AI_MODEL_TESTING_COMPLETE.md) مراجعه کنید.

