# 📖 راهنمای استفاده از نمایش تشخیص AI

## 🎯 برای کاربران (دکترها)

### 1. مشاهده نتایج AI

#### الف) باز کردن صفحه بیمار:
```
داشبورد → ارتودنسی → انتخاب بیمار → تب "تشخیص AI"
```

#### ب) اجرای تحلیل AI:
1. مطمئن شوید تصاویر بیمار آپلود شده (داخل دهانی، خارج دهانی، رادیولوژی)
2. (اختیاری) مدل AI را انتخاب کنید (GPT-4o پیشنهادی)
3. روی دکمه **"تحلیل کامل با AI"** کلیک کنید
4. صبر کنید تا تحلیل تکمیل شود (30-60 ثانیه)

#### ج) مشاهده نتایج:
نتایج به صورت زیر نمایش داده می‌شود:

```
┌─────────────────────────────────────┐
│  ⭐ خلاصه تشخیص AI                 │
│  (کارت آبی با متن خلاصه)            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🔑 یافته‌های کلیدی                │
│  [Chip 1] [Chip 2] [Chip 3]        │
└─────────────────────────────────────┘

▼ 📄 تحلیل داخل دهانی
  (کلیک کنید تا باز شود)

▼ 👤 تحلیل خارج دهانی
  (کلیک کنید تا باز شود)

▼ 📷 تحلیل رادیولوژی
  (کلیک کنید تا باز شود)

... و سایر بخش‌ها
```

---

### 2. ویرایش نتایج

#### الف) ویرایش خلاصه:
1. در کارت آبی "خلاصه تشخیص"
2. کلیک روی **"ویرایش خلاصه"**
3. متن را در دیالوگ باز شده ویرایش کنید
4. **"ذخیره تغییرات"** را بزنید

#### ب) ویرایش هر بخش:
1. بخش مورد نظر را باز کنید (مثلاً "تحلیل داخل دهانی")
2. پایین بخش، روی **"ویرایش این بخش"** کلیک کنید
3. متن را ویرایش کنید
4. **"ذخیره تغییرات"** را بزنید

#### ج) نکات مهم:
- تغییرات فوراً در صفحه اعمال می‌شوند
- برای ذخیره نهایی در دیتابیس، دکمه **"ذخیره نهایی"** را بزنید
- تعداد کلمات در حین ویرایش نمایش داده می‌شود

---

### 3. ابزارهای کاربردی

در پایین صفحه، دکمه‌های زیر موجود است:

#### 🖨️ **پرینت تحلیل**:
- پرینت کامل تمام نتایج
- فرمت بندی مناسب برای چاپ

#### 📥 **دانلود متن**:
- دانلود به صورت فایل `.txt`
- شامل تمام بخش‌ها

#### 📋 **کپی همه**:
- کپی کامل متن به clipboard
- قابل paste در Word یا نرم‌افزارهای دیگر

#### 💾 **ذخیره نهایی**:
- ذخیره تمام تغییرات در دیتابیس
- نمایش پیام موفقیت

---

### 4. رنگ‌بندی Chips یافته‌های کلیدی

Chips به صورت خودکار رنگ‌بندی می‌شوند:

```
🔴 قرمز:  موارد شدید (severe)
🟡 زرد:   موارد متوسط (moderate)
🟢 سبز:   موارد عادی یا خفیف
```

این به شما کمک می‌کند سریعاً موارد مهم را شناسایی کنید.

---

### 5. کارکرد با بخش‌های مختلف

#### 📄 خلاصه تشخیص:
- **محتوا**: خلاصه کلی مشکلات و یافته‌های اصلی
- **رنگ**: آبی (primary)
- **موقعیت**: بالای صفحه، همیشه نمایان

#### 🦷 تحلیل داخل دهانی:
- **محتوا**: وضعیت دندان‌ها، لثه، occlusion، crowding
- **رنگ**: آبی روشن (info)
- **موقعیت**: Accordion اول

#### 👤 تحلیل خارج دهانی:
- **محتوا**: symmetry صورت، facial profile, lip position
- **رنگ**: سبز (success)
- **موقعیت**: Accordion دوم

#### 📷 تحلیل رادیولوژی:
- **محتوا**: وضعیت استخوان، ریشه‌ها، پاتولوژی‌ها
- **رنگ**: زرد (warning)
- **موقعیت**: Accordion سوم

#### 📏 تحلیل سفالومتری:
- **محتوا**: اندازه‌گیری‌های سفالومتریک، زوایا
- **رنگ**: بنفش (secondary)
- **موقعیت**: Accordion چهارم

#### 💊 توصیه‌های درمانی:
- **محتوا**: پلان درمان، اولویت‌ها، پیش‌بینی
- **رنگ**: قرمز (error)
- **موقعیت**: Accordion آخر

---

### 6. نکات و ترفندها

#### ✅ برای مشاهده سریع:
- فقط خلاصه و یافته‌های کلیدی را ببینید
- بخش‌ها را فقط در صورت نیاز باز کنید

#### ✅ برای ویرایش دقیق:
- هر بخش را جداگانه ویرایش کنید
- از نمایش تعداد کلمات استفاده کنید

#### ✅ برای ارائه به بیمار:
- از پرینت برای ارائه فیزیکی
- از دانلود برای ارسال ایمیل

#### ✅ برای یادداشت‌برداری:
- مستقیماً در بخش‌های مختلف بنویسید
- نکات اضافی خود را اضافه کنید

---

### 7. مثال‌های کاربردی

#### مثال 1: تصحیح نتایج AI
```
AI می‌گوید: "crowding شدید"
شما می‌توانید تغییر دهید به: "crowding متوسط در فک پایین"
```

#### مثال 2: افزودن جزئیات
```
AI می‌گوید: "crossbite قدامی"
شما می‌توانید اضافه کنید: "crossbite قدامی دندان‌های 11 و 21"
```

#### مثال 3: افزودن توصیه‌های شخصی
```
AI می‌گوید: "نیاز به براکت"
شما می‌توانید اضافه کنید: "براکت فلزی یا سرامیکی - بر اساس انتخاب بیمار"
```

---

### 8. خطایابی (Troubleshooting)

#### مشکل: "هنوز تحلیل AI انجام نشده"
**راه‌حل**: 
- تصاویر بیمار را آپلود کنید
- روی دکمه "تحلیل کامل با AI" کلیک کنید

#### مشکل: "نتایج خالی است"
**راه‌حل**:
- اجرای مجدد تحلیل
- بررسی اینترنت و اتصال به سرور

#### مشکل: "تغییرات ذخیره نمی‌شود"
**راه‌حل**:
- روی "ذخیره نهایی" کلیک کنید (نه فقط ویرایش)
- بررسی پیام‌های خطا در console

#### مشکل: "بخش‌ها خالی هستند"
**راه‌حل**:
- AI ممکن است بدون ساختار جواب داده باشد
- خلاصه را ببینید که کل متن در آنجاست
- می‌توانید دستی بخش‌بندی کنید

---

### 9. میانبرهای کیبورد (برای نسخه‌های آینده)

```
Ctrl + P  →  پرینت
Ctrl + S  →  ذخیره
Ctrl + C  →  کپی
Ctrl + E  →  ویرایش
Escape    →  بستن دیالوگ
```

---

### 10. سوالات متداول (FAQ)

**Q: آیا می‌توانم نتایج AI را کاملاً حذف کنم؟**
A: بله، می‌توانید هر بخش را خالی کنید یا اجرای مجدد تحلیل کنید.

**Q: آیا تغییرات من روی AI تاثیر می‌گذارد؟**
A: نه، تغییرات شما فقط برای این بیمار ذخیره می‌شود و روی مدل AI تاثیری ندارد.

**Q: می‌توانم چند بار تحلیل کنم؟**
A: بله، هر بار تحلیل قبلی جایگزین می‌شود. پیشنهاد: تغییرات مهم را ذخیره کنید.

**Q: آیا بیمار می‌تواند این نتایج را ببیند؟**
A: بستگی به تنظیمات سیستم دارد. معمولاً فقط پزشک دسترسی دارد.

**Q: فرق "ذخیره تغییرات" و "ذخیره نهایی" چیست؟**
A: "ذخیره تغییرات" در دیالوگ ویرایش، فقط دیالوگ را می‌بندد. "ذخیره نهایی" در دیتابیس ذخیره می‌کند.

---

## 🎓 برای توسعه‌دهندگان

### استفاده از کامپوننت:

```jsx
import AIDiagnosisDisplay from './ai-diagnosis-display';

// در کامپوننت شما:
<AIDiagnosisDisplay
  diagnosis={patient.aiDiagnosis}
  onUpdate={(updated) => setPatient({...patient, aiDiagnosis: updated})}
  onSave={handleSave}
  readOnly={false}
/>
```

### Props:

```typescript
interface AIDiagnosisDisplayProps {
  diagnosis: string;          // متن کامل تشخیص AI
  onUpdate?: (text: string) => void;  // Callback برای update
  onSave?: () => void;        // Callback برای save نهایی
  readOnly?: boolean;         // غیرفعال کردن ویرایش (default: false)
}
```

### Customization:

```jsx
// تغییر رنگ‌ها:
const sectionConfig = [
  { key: 'summary', color: 'primary' },
  // ...
];

// افزودن بخش جدید:
const sectionConfig = [
  ...existing,
  { key: 'newSection', title: 'بخش جدید', icon: 'icon-name', color: 'info' }
];
```

---

## 📞 پشتیبانی

اگر مشکلی داشتید یا سوالی دارید:

1. **مستندات**: این فایل و `AI_DIAGNOSIS_IMPROVEMENTS.md`
2. **کد**: فایل `ai-diagnosis-display.jsx`
3. **تیکت**: ثبت تیکت در سیستم
4. **ایمیل**: support@dentalai.com

---

**نسخه:** 1.0.0  
**تاریخ:** 2025-10-30  
**وضعیت:** ✅ آماده استفاده

---

**لذت ببرید از نمایش حرفه‌ای تشخیص AI!** 🎉




















