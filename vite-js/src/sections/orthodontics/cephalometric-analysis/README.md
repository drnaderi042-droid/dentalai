# ابزار تحلیل سفالومتری (Cephalometric Analysis Tool)

این ابزار یک سیستم جامع برای تحلیل تصاویر سفالومتری در پروژه‌های دندانپزشکی ارتودنسی است که به سبک WebCeph Analysis طراحی شده است.

## 📋 فهرست مطالب

1. [ویژگی‌های کلیدی](#ویژگی‌های-کلیدی)
2. [نحوه استفاده](#نحوه-استفاده)
3. [کامپوننت‌ها](#کامپوننت‌ها)
4. [اندازه‌گیری‌ها](#اندازه‌گیری‌ها)
5. [مسیریابی](#مسیریابی)

## 🎯 ویژگی‌های کلیدی

### 1. **کانوس تعاملی سفالومتری**
- نقطگذاری دقیق لندمارک‌های آناتومیکی
- زوم و پن روی تصویر
- رسم خطوط اندازه‌گیری خودکار
- نمایش برچسب‌های لندمارک

### 2. **محاسبات خودکار**
- **اندازه‌گیری‌های اسکلتی:**
  - SNA: زاویه Sella-Nasion-A point
  - SNB: زاویه Sella-Nasion-B point
  - ANB: تفاوت بین SNA و SNB
  - Gonial Angle: زاویه گونیال

- **اندازه‌گیری‌های دندانی:**
  - Overjet: فاصله افقی بین incisors
  - Interincisal Angle: زاویه بین incisors

### 3. **نمایش نتایج**
- **Combination Factor:** شاخص ترکیبی SNA + SNB
- نمایش وضعیت (نرمال، بالا، پایین)
- نمودار Linear Progress برای مقایسه با مقادیر نرمال
- سه تب: Line Analysis، Profilogram، Chart

### 4. **قابلیت‌های پیشرفته**
- ذخیره تحلیل در دیتابیس
- چاپ گزارش
- خروجی Excel
- آپلود تصاویر جدید

## 🚀 نحوه استفاده

### مرحله 1: دسترسی به ابزار

از صفحه مدیریت بیمار، روی دکمه **"ابزار تحلیل سفالومتری WebCeph"** کلیک کنید:

```
مسیر: /dashboard/orthodontics/patient/:id → تب "آنالیز سفالومتری"
```

### مرحله 2: آپلود تصویر

1. اگر تصویر سفالومتری وجود ندارد، روی دکمه "انتخاب تصویر" کلیک کنید
2. تصویر رادیوگرافی جانبی جمجمه (Lateral Cephalogram) را انتخاب کنید
3. تصویر به طور خودکار بارگذاری می‌شود

### مرحله 3: نقطگذاری لندمارک‌ها

1. از لیست نقاط، یک لندمارک را انتخاب کنید (مثلاً "S" برای Sella)
2. روی تصویر در محل دقیق لندمارک کلیک کنید
3. این کار را برای تمام لندمارک‌های مورد نیاز تکرار کنید

**لندمارک‌های اصلی:**
- **S** (Sella): مرکز sella turcica
- **N** (Nasion): نقطه قدامی suture nasofrontal
- **A** (Point A): نقطه عمیق‌ترین قدامی ماگزیلا
- **B** (Point B): نقطه عمیق‌ترین قدامی مندیبل
- **Pog** (Pogonion): نقطه قدامی‌ترین چانه
- **Go** (Gonion): زاویه مندیبل
- **Or** (Orbitale): نقطه پایین‌ترین حاشیه orbital
- **Po** (Porion): نقطه بالاترین meatus auditory

### مرحله 4: کنترل‌های تصویر

- **زوم:** استفاده از چرخ ماوس
- **پن:** نگه داشتن Ctrl + کشیدن ماوس
- **ریست:** کلیک روی آیکون Refresh

### مرحله 5: مشاهده نتایج

پس از نقطگذاری لندمارک‌ها:
1. اندازه‌گیری‌ها به طور خودکار محاسبه می‌شوند
2. در پنل سمت راست نمایش داده می‌شوند
3. Combination Factor محاسبه و نمایش داده می‌شود

### مرحله 6: ذخیره تحلیل

روی دکمه **"ذخیره تحلیل"** کلیک کنید تا نتایج در دیتابیس ذخیره شوند.

## 🧩 کامپوننت‌ها

### 1. `CephalometricCanvas`
**مسیر:** `./cephalometric-canvas.jsx`

کانوس اصلی برای نمایش تصویر و نقطگذاری لندمارک‌ها.

**Props:**
```javascript
{
  imageUrl: string,              // آدرس تصویر سفالومتری
  onMeasurementsChange: function // callback برای تغییرات اندازه‌گیری
}
```

**ویژگی‌ها:**
- نمایش تصویر با Canvas API
- نقطگذاری تعاملی
- رسم خطوط اندازه‌گیری
- زوم و پن

### 2. `AnalysisResultsPanel`
**مسیر:** `./analysis-results-panel.jsx`

پنل نمایش نتایج تحلیل.

**Props:**
```javascript
{
  measurements: object  // شیء حاوی اندازه‌گیری‌ها
}
```

**ویژگی‌ها:**
- نمایش Combination Factor
- لیست اندازه‌گیری‌های اسکلتی و دندانی
- نمودار مقایسه با مقادیر نرمال
- سه تب: Line Analysis، Profilogram، Chart

### 3. `CephalometricAnalysisView`
**مسیر:** `./cephalometric-analysis-view.jsx`

کامپوننت اصلی صفحه تحلیل.

**ویژگی‌ها:**
- مدیریت state بیمار و اندازه‌گیری‌ها
- آپلود تصویر
- ذخیره تحلیل در API
- نمایش راهنمای استفاده

## 📊 اندازه‌گیری‌ها

### مقادیر نرمال

```javascript
const NORMAL_RANGES = {
  SNA: { mean: 82, sd: 3.5, min: 78.5, max: 85.5 },
  SNB: { mean: 80, sd: 3.5, min: 76.5, max: 83.5 },
  ANB: { mean: 2, sd: 2, min: 0, max: 4 },
  gonialAngle: { mean: 120, sd: 5, min: 115, max: 125 },
  FMA: { mean: 25, sd: 4, min: 21, max: 29 },
  IMPA: { mean: 90, sd: 3, min: 87, max: 93 },
  interincisalAngle: { mean: 130, sd: 6, min: 124, max: 136 },
  overjet: { mean: 3, sd: 1, min: 2, max: 4 },
  overbite: { mean: 3, sd: 1, min: 2, max: 4 },
  combinationFactor: { mean: 157.9, sd: 6.5, min: 151.4, max: 164.4 }
}
```

### محاسبات

**زاویه‌ها:**
```javascript
const calculateAngle = (p1, vertex, p2) => {
  const angle1 = Math.atan2(p1.y - vertex.y, p1.x - vertex.x);
  const angle2 = Math.atan2(p2.y - vertex.y, p2.x - vertex.x);
  let angle = Math.abs(angle1 - angle2) * (180 / Math.PI);
  if (angle > 180) angle = 360 - angle;
  return angle.toFixed(2);
}
```

**فاصله:**
```javascript
const calculateDistance = (p1, p2) => {
  const dx = p2.x - p1.x;
  const dy = p2.y - p1.y;
  return Math.sqrt(dx * dx + dy * dy).toFixed(2);
}
```

## 🛣️ مسیریابی

### مسیر اصلی
```
/dashboard/orthodontics/patient/:patientId/cephalometric-analysis
```

### تنظیمات مسیر

**در `routes/sections/dashboard.jsx`:**
```javascript
{ 
  path: 'orthodontics/patient/:patientId/cephalometric-analysis', 
  element: <CephalometricAnalysisPage /> 
}
```

**در `routes/paths.js`:**
```javascript
patient: {
  orthodontics: (id) => `${ROOTS.DASHBOARD}/orthodontics/patient/${id}`,
  cephalometricAnalysis: (patientId) => 
    `${ROOTS.DASHBOARD}/orthodontics/patient/${patientId}/cephalometric-analysis`,
}
```

## 🔧 نصب و راه‌اندازی

این ابزار بخشی از پروژه اصلی است و نیاز به نصب جداگانه ندارد. برای استفاده:

1. مطمئن شوید که پروژه اصلی در حال اجرا است
2. به صفحه بیماران مراجعه کنید
3. یک بیمار را انتخاب کنید
4. به تب "آنالیز سفالومتری" بروید
5. روی دکمه "ابزار تحلیل سفالومتری WebCeph" کلیک کنید

## 📝 نکات مهم

1. **کیفیت تصویر:** برای نتایج بهتر، از تصاویر با کیفیت بالا استفاده کنید
2. **دقت نقطگذاری:** لندمارک‌ها را دقیقاً در محل صحیح قرار دهید
3. **ذخیره منظم:** پس از تکمیل تحلیل، حتماً ذخیره کنید
4. **مرورگر:** از مرورگرهای مدرن (Chrome، Firefox، Edge) استفاده کنید

## 🐛 عیب‌یابی

### تصویر نمایش داده نمی‌شود
- مطمئن شوید که تصویر در فرمت صحیح (PNG، JPG) است
- بررسی کنید که سرور در حال اجرا باشد
- cache مرورگر را پاک کنید

### اندازه‌گیری‌ها محاسبه نمی‌شوند
- حداقل نقاط مورد نیاز را مشخص کنید (S، N، A برای SNA)
- console browser را برای خطاها بررسی کنید

### ذخیره انجام نمی‌شود
- اتصال به اینترنت را بررسی کنید
- مطمئن شوید که لاگین هستید
- وضعیت API را بررسی کنید

## 📚 منابع اضافی

- [استانداردهای سفالومتری](https://example.com)
- [راهنمای جامع ارتودنسی](https://example.com)
- [WebCeph Official](https://webceph.com)

## 👨‍💻 توسعه‌دهندگان

برای توسعه‌دهندگان که می‌خواهند این ابزار را توسعه دهند:

### ساختار فایل‌ها
```
cephalometric-analysis/
├── cephalometric-canvas.jsx          # کانوس اصلی
├── analysis-results-panel.jsx        # پنل نتایج
├── cephalometric-analysis-view.jsx   # view اصلی
├── index.js                          # exports
└── README.md                         # این فایل
```

### اضافه کردن لندمارک جدید

در `cephalometric-canvas.jsx`:
```javascript
const LANDMARKS = {
  ...existing,
  NEW_LANDMARK: { 
    name: 'New Landmark', 
    x: 0, 
    y: 0, 
    color: '#color' 
  }
}
```

### اضافه کردن اندازه‌گیری جدید

در `analysis-results-panel.jsx`:
```javascript
const NORMAL_RANGES = {
  ...existing,
  newMeasurement: { 
    mean: value, 
    sd: value, 
    min: value, 
    max: value 
  }
}
```

---

**نسخه:** 1.0.0  
**آخرین بروزرسانی:** 2025  
**پشتیبانی:** برای سوالات و مشکلات، یک issue در GitHub ایجاد کنید