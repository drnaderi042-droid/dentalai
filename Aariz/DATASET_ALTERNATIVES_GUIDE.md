# راهنمای استفاده از دیتاست‌ها و مدل‌های آماده

## 📋 خلاصه

متأسفانه **دیتاست‌های عمومی با لندمارک‌های بیشتر از 29 (مثل PT - Pterygoid) به راحتی در دسترس نیستند**. اما راه‌حل‌های جایگزین وجود دارد.

## 🔍 دیتاست‌های موجود

### 1. **Aariz Dataset** (که شما دارید)
- ✅ 29 لندمارک
- ✅ 1000 تصویر
- ✅ قابل گسترش

### 2. **ISBI 2015 Challenge**
- ⚠️ 19 لندمارک
- ⚠️ نیاز به ثبت‌نام

### 3. **MICCAI CLdetection2023**
- ⚠️ 19 لندمارک  
- ⚠️ نیاز به درخواست

## 💡 راه‌حل پیشنهادی: Semi-Supervised Learning

به جای آنوتیت کامل، از **Semi-Supervised Learning** استفاده کنید:

### مرحله 1: پیش‌بینی با مدل موجود

```bash
python semi_supervised_prediction.py \
    --checkpoint checkpoints/checkpoint_best_512x512.pth \
    --additional_landmarks PT \
    --output_dir predictions_new_landmarks \
    --uncertainty_threshold 0.5
```

این اسکریپت:
1. مدل موجود را روی تصاویر بدون آنوتیت اجرا می‌کند
2. لندمارک‌های جدید را پیش‌بینی می‌کند
3. Uncertainty score محاسبه می‌کند
4. تصاویر با uncertainty بالا را برای بررسی دستی مشخص می‌کند

### مرحله 2: بررسی و تصحیح دستی

فقط تصاویر با **uncertainty بالا** را بررسی و تصحیح کنید (نه همه تصاویر).

### مرحله 3: Fine-tuning

از داده‌های تصحیح شده برای fine-tuning استفاده کنید.

## 📊 مزایای این روش

✅ **کاهش زمان آنوتیت**: فقط موارد مشکوک را بررسی می‌کنید  
✅ **استفاده از مدل موجود**: از دانش مدل استفاده می‌شود  
✅ **تدریجی**: می‌توانید به تدریج داده اضافه کنید  

## 🚀 استفاده

```bash
# 1. پیش‌بینی
python semi_supervised_prediction.py \
    --checkpoint checkpoints/checkpoint_best.pth \
    --additional_landmarks PT

# 2. بررسی فایل high_uncertainty.json
# 3. تصحیح موارد لازم
# 4. Fine-tuning با داده‌های جدید
python finetune_extended_landmarks.py \
    --checkpoint checkpoints/checkpoint_best.pth \
    --additional_landmarks PT
```
















