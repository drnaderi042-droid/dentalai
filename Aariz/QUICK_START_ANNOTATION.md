# 🚀 شروع سریع - Annotation برای p1/p2

## ✅ فقط 3 مرحله!

### 1️⃣ اجرای برنامه

```cmd
cd aariz
annotate_p1_p2.bat
```

**همین!** برنامه با تنظیمات پیش‌فرض شروع می‌شود:
- 📁 **مسیر**: `Aariz/train/Cephalograms`
- 💾 **خروجی**: `annotations_p1_p2.json`
- 🔢 **تعداد**: اولین 100 تصویر

---

### 2️⃣ Annotation

1. **کلیک اول**: روی نقطه **بالای** خطکش (p2 - 1cm mark)
2. **کلیک دوم**: روی نقطه **پایین** خطکش (p1 - 0cm mark)
3. فشار دادن **'n'**: برای رفتن به تصویر بعدی

```
┌─────────┐
│  [p2]   │  ← کلیک اول
│         │
│   10mm  │
│         │
│  [p1]   │  ← کلیک دوم
└─────────┘
```

**خودکار ذخیره می‌شود!** ✅

---

### 3️⃣ بررسی کیفیت (اختیاری)

```cmd
python check_annotations_quality.py annotations_p1_p2.json
```

این نشان می‌دهد:
- ✅ تعداد annotations کامل
- ✅ میانگین دقت
- ⚠️ annotations مشکوک

---

## ⌨️ کلیدهای مهم

| کلید | عملکرد |
|------|--------|
| **کلیک موس** | انتخاب p2 سپس p1 |
| **n** | تصویر بعدی |
| **p** | تصویر قبلی |
| **r** | پاک کردن |
| **+** | بزرگ‌نمایی |
| **q** | خروج و ذخیره |

---

## 🎯 نکات سریع

✅ **دقت:** از zoom استفاده کنید (`+` کلید)

✅ **ترتیب:** اول p2 (بالا) بعد p1 (پایین)

✅ **Auto-save:** نگران نباشید، همیشه ذخیره می‌شود

✅ **Resume:** می‌توانید هر وقت بخواهید ادامه دهید

---

## 🔧 تنظیمات سفارشی (اختیاری)

### مسیر دیگر:

```cmd
annotate_p1_p2.bat "path/to/my/images"
```

### فایل خروجی دیگر:

```cmd
annotate_p1_p2.bat "Aariz/train/Cephalograms" "my_custom_output.json"
```

### تعداد تصاویر بیشتر:

```cmd
annotate_p1_p2.bat "Aariz/train/Cephalograms" "annotations_p1_p2.json" 200
```

یا مستقیماً:

```cmd
python p1_p2_annotator.py --max-images 200
```

---

## 📊 بعد از Annotation

### آموزش مدل:

```cmd
# استفاده از annotations جدید برای training
train_hrnet.bat
```

مدل خودکار از `annotations_p1_p2.json` استفاده می‌کند! ✅

---

## ❓ سوالات متداول

### چند تا عکس باید annotate کنم؟

| تعداد | نتیجه |
|-------|-------|
| 20-30 | تست اولیه |
| 50-100 | خوب ✅ |
| 100+ | عالی 🌟 |
| 200+ | بهترین 🚀 |

### چقدر طول می‌کشد؟

- ⏱️ **هر عکس:** ~10 ثانیه
- ⏱️ **100 عکس:** ~15-20 دقیقه

### اگر اشتباه کردم؟

```
فشار 'r' → پاک می‌شود
کلیک مجدد → annotate دوباره
```

### اگر برنامه بسته شد؟

```cmd
# دوباره اجرا کنید
annotate_p1_p2.bat

# از همان‌جا ادامه می‌دهد! ✅
```

---

## 📖 راهنمای کامل

برای اطلاعات بیشتر: [P1_P2_ANNOTATION_GUIDE.md](P1_P2_ANNOTATION_GUIDE.md)

---

**شروع کنید:**

```cmd
cd aariz
annotate_p1_p2.bat
```

**موفق باشید! 🎉**













