# ⚡ تست سریع مدل 512×512

## 📊 نتایج Training

بر اساس خروجی training:
- **MRE**: **1.41mm** ✅ (خیلی خوب!)
- **SDR @ 2mm**: **80.25%** ✅ (عالی!)
- **SDR @ 2.5mm**: 86.67%
- **SDR @ 3mm**: 90.51%

---

## 🚀 تست مدل

### روش 1: استفاده از Batch File (ساده‌ترین)

```batch
Aariz\test_512x512.bat
```

### روش 2: دستی در PowerShell

```powershell
cd "C:\Users\Salah\Downloads\Compressed\Dentalai\main - Copy\Aariz"
python test_512x512_model.py
```

---

## 📝 اسکریپت تست چه می‌کند؟

1. ✅ مدل 512×512 را از `checkpoints/checkpoint_best.pth` بارگذاری می‌کند
2. ✅ تصویر تست را با **512×512** پردازش می‌کند (نه 256×256!)
3. ✅ با Ground Truth مقایسه می‌کند
4. ✅ MRE و SDR را محاسبه می‌کند
5. ✅ نتایج را با نتایج training مقایسه می‌کند

---

## 📊 نتایج انتظاری

### اگر مدل خوب train شده باشد:
- **MRE**: ~1.4-1.6mm (مشابه validation set)
- **SDR @ 2mm**: ~75-82%
- **تفاوت با training**: ±0.2mm (طبیعی)

### اگر تفاوت زیاد باشد:
- ممکن است checkpoint قدیمی باشد (256×256)
- یا مشکل دیگری وجود دارد

---

## ⚠️ نکات مهم

1. **Checkpoint باید 512×512 باشد**: اگر از checkpoint قدیمی (256×256) استفاده کنید، نتایج بد خواهند بود
2. **API**: `app_aariz.py` به‌روز شده و از 512×512 استفاده می‌کند
3. **Frontend**: اگر از frontend استفاده می‌کنید، نیازی به تغییر نیست (API خودش مدیریت می‌کند)

---

## 🔄 اگر نتایج بد بودند

### بررسی کنید:

1. **Checkpoint درست است؟**
   ```powershell
   # بررسی آخرین checkpoint
   ls checkpoints\checkpoint_best.pth -Recurse | Sort-Object LastWriteTime -Descending | Select-Object -First 1
   ```

2. **Training تمام شده؟**
   - باید checkpoint جدید با MRE ~1.4mm باشد
   - اگر training هنوز در حال اجرا است، صبر کنید تا تمام شود

3. **Image size در کد درست است؟**
   - `test_512x512_model.py` استفاده می‌کند از `target_size=(512, 512)` ✅

---

## 📈 مقایسه با نتایج قبلی

| معیار | 256×256 (قبل) | 512×512 (الان) | بهبود |
|-------|--------------|---------------|-------|
| **MRE** | 1.99mm | ~1.4mm | ✅ بهتر |
| **SDR @ 2mm** | 65.52% | ~80% | ✅ +15% |

---

**شروع کنید با:** `Aariz\test_512x512.bat` 🚀

