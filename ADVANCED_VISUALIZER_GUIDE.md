 # 🎨 راهنمای Advanced Cephalometric Visualizer

## ✅ قابلیت‌های جدید اضافه شده

همه قابلیت‌های درخواستی شما به صفحه `/dashboard/ai-model-test` اضافه شد:

1. ✅ **تغییر ضخامت و رنگ خطوط**
2. ✅ **Drag کردن نقاط با موس**
3. ✅ **تغییر رنگ نقاط**
4. ✅ **افکت Scale با Hover**
5. ✅ **محاسبه زاویه‌ها** (SNA, SNB, ANB, FMA, GoGnSN, U1-SN, Interincisal Angle)
6. ✅ **اضافه کردن نقطه دلخواه**
7. ✅ **حذف نقطه**
8. ✅ **Tracing حدود ساختارها** (مولر، سانترال، و...)

---

## 🚀 چطور استفاده کنیم؟

### 1️⃣ تنظیمات اولیه:

#### A) اندازه و ظاهر نقاط:
```
📏 اندازه نقطه: 4 تا 20 پیکسل
🎨 اندازه خط: 1 تا 10 پیکسل
🌈 رنگ خط: انتخاب از color picker
```

#### B) نمایش:
```
✅ برچسب‌ها: نمایش/مخفی کردن نام نقاط
✅ خطوط: نمایش/مخفی کردن خطوط بین نقاط
```

---

### 2️⃣ جابجایی نقاط (Drag & Drop):

```
1. موس را روی نقطه ببرید → افکت Scale می‌بینید! ✨
2. کلیک کرده و نگه دارید
3. نقطه را به جای دلخواه بکشید
4. موس را رها کنید
```

**افکت Hover:**
- وقتی موس روی نقطه باشد، اندازه نقطه 1.5x بزرگ‌تر می‌شود
- افکت smooth transition دارد
- نقطه انتخاب شده با حاشیه طلایی نمایش داده می‌شود

---

### 3️⃣ افزودن نقطه جدید:

```
1. سوئیچ "افزودن نقطه" را فعال کنید
2. در فیلد text، نام نقطه را وارد کنید (مثلاً: M1, M2, C1)
3. روی تصویر کلیک کنید
4. نقطه جدید اضافه می‌شود!
```

**مثال - افزودن نقاط مولر:**
```
- فعال کنید: "افزودن نقطه"
- نام: UM6 (Upper Molar 6)
- کلیک روی مولر فک بالا
- نام: LM6 (Lower Molar 6)
- کلیک روی مولر فک پایین
```

---

### 4️⃣ حذف نقطه:

**روش 1: کلیک راست**
```
1. روی نقطه راست‌کلیک کنید
2. از منو "حذف [نام نقطه]" را انتخاب کنید
```

**روش 2: دستی**
```
از کد JSON که نمایش داده می‌شود، می‌توانید landmarks را ویرایش کنید
```

---

### 5️⃣ محاسبه زاویه‌ها:

**زاویه‌های محاسبه شده:**

```
📐 SNA (Sella-Nasion-A Point)
   • طبیعی: 82° ± 2°
   • موقعیت maxilla

📐 SNB (Sella-Nasion-B Point)
   • طبیعی: 80° ± 2°
   • موقعیت mandible

📐 ANB (A-Nasion-B)
   • طبیعی: 2° ± 2°
   • رابطه skeletal

📐 FMA (Frankfort-Mandibular Angle)
   • طبیعی: 25° ± 5°
   • الگوی رشد عمودی

📐 GoGnSN
   • طبیعی: 32° ± 2°
   • زاویه mandibular plane

📐 U1-SN
   • طبیعی: 103° ± 5°
   • زاویه دندان فک بالا

📐 Interincisal Angle
   • طبیعی: 130° ± 10°
   • رابطه دندان‌ها
```

**زاویه‌ها به صورت خودکار محاسبه می‌شوند:**
```
✅ وقتی landmarks موجود باشند
✅ به صورت realtime update می‌شوند
✅ در کارت "محاسبات Cephalometric" نمایش داده می‌شوند
```

---

### 6️⃣ Tracing حدود ساختارها (⭐ ویژگی جدید!):

این قابلیت برای ترسیم حدود دندان‌ها و سایر ساختارها است:

#### راه‌اندازی Tracing:

```
1. سوئیچ "Tracing ساختارها" را فعال کنید
2. نام ساختار را وارد کنید:
   - "Upper Molar 6" → حدود مولر فک بالا
   - "Lower Molar 6" → حدود مولر فک پایین
   - "Upper Incisor" → حدود سانترال فک بالا
   - "Lower Incisor" → حدود سانترال فک پایین
   - "Mandibular Outline" → حدود فک پایین
   - و...
```

#### ترسیم:

```
3. روی تصویر کلیک کنید
   → هر کلیک یک نقطه به trace اضافه می‌کند
   
4. نقاط را دور ساختار کلیک کنید
   → خطوط سبز رنگ به صورت realtime نمایش داده می‌شوند
   
5. دکمه "ذخیره" را بزنید
   → ساختار ذخیره می‌شود و رنگ بنفش می‌شود
   
6. برای ساختار بعدی، مراحل را تکرار کنید
```

#### مثال عملی - ترسیم مولر فک بالا:

```
Step 1: فعال کنید "Tracing ساختارها"
Step 2: نام: "Upper Molar Crown"
Step 3: کلیک‌های پیاپی:
   - گوشه mesial
   - cusp buccal
   - گوشه distal
   - cusp lingual
   - بازگشت به mesial
Step 4: ذخیره → شکل بسته ترسیم می‌شود
```

#### پاک کردن Tracing:

```
- دکمه "پاک کردن" → فقط نقاط فعلی را پاک می‌کند
- آیکون سطل زباله کنار هر ساختار → آن ساختار را حذف می‌کند
```

#### نکات مهم Tracing:

```
✅ می‌توانید چند ساختار مختلف بکشید
✅ هر ساختار رنگ جداگانه دارد
✅ ساختارها به صورت خودکار بسته می‌شوند
✅ می‌توانید روی آن‌ها zoom/pan کنید
```

---

### 7️⃣ Zoom و Pan:

```
🔍 Zoom In/Out: دکمه‌های + و -
🔍 Mouse Wheel: برای zoom سریع
🖐️ Pan: کلیک + بکشید (در جای خالی)
🔄 Reset: بازگشت به حالت اولیه
```

---

## 🎯 Use Case‌های واقعی:

### ✅ Use Case 1: تصحیح نقاط نادرست

```
مشکل: مدل AI نقطه A را اشتباه تشخیص داده
راه حل:
  1. موس روی نقطه A → افکت hover
  2. کلیک و drag به جای درست
  3. رها کنید → نقطه جابجا شد!
  4. زاویه‌ها به صورت خودکار update می‌شوند
```

### ✅ Use Case 2: افزودن نقاط اضافی

```
نیاز: می‌خواهید نقاط مولر را اضافه کنید
راه حل:
  1. فعال: "افزودن نقطه"
  2. نام: M6U (Molar 6 Upper)
  3. کلیک روی مولر
  4. نقطه اضافه شد!
  5. همین کار را برای M6L تکرار کنید
```

### ✅ Use Case 3: ترسیم outline دندان

```
نیاز: می‌خواهید حدود دندان central را بکشید
راه حل:
  1. فعال: "Tracing ساختارها"
  2. نام: "Upper Central Incisor"
  3. کلیک‌های پیاپی دور دندان
  4. ذخیره
  5. حدود دندان ترسیم شد!
```

### ✅ Use Case 4: حذف نقاط اضافی

```
مشکل: نقطه‌ای اضافی تشخیص داده شده
راه حل:
  1. راست‌کلیک روی نقطه
  2. انتخاب "حذف"
  3. نقطه حذف شد!
```

---

## 📊 نمایش محاسبات:

```
وقتی نقاط کافی باشند، کارت محاسبات نمایش داده می‌شود:

┌─────────────────────────────────┐
│ 📐 محاسبات Cephalometric       │
├─────────────────────────────────┤
│ SNA: 82.5°                      │
│ SNB: 80.2°                      │
│ ANB: 2.3°                       │
│ FMA: 25.0°                      │
│ GoGnSN: 32.0°                   │
│ U1-SN: 103.5°                   │
│ Interincisal Angle: 130.0°      │
└─────────────────────────────────┘
```

---

## 🎨 سفارشی‌سازی رنگ‌ها:

### رنگ‌بندی پیش‌فرض:

```
🔵 آبی: نقاط اسکلتی (S, N, A, B, Pog, Go, Me, ...)
🔴 قرمز: نقاط دندانی (U1, L1)
🟠 نارنجی: نقاط soft tissue (UL, LL, Sn, PogSoft)
🟢 سبز: سایر نقاط
```

### تغییر رنگ خطوط:

```
1. کنار "رنگ خط" روی color picker کلیک کنید
2. رنگ دلخواه را انتخاب کنید
3. همه خطوط به رنگ جدید تغییر می‌کنند
```

---

## 💾 ذخیره و بازیابی:

### ذخیره خودکار:

```
✅ هر تغییری که در landmarks بدهید، به result اصلی sync می‌شود
✅ می‌توانید JSON را کپی یا دانلود کنید
✅ تاریخچه در database ذخیره می‌شود
```

### Export:

```
1. دکمه "کپی JSON" → کپی در clipboard
2. دکمه "دانلود" → فایل JSON
```

---

## 🔧 تنظیمات پیشرفته:

### تنظیمات Visual:

| تنظیم | دامنه | پیش‌فرض | توضیح |
|-------|-------|---------|-------|
| اندازه نقطه | 4-20px | 8px | اندازه نقاط landmarks |
| ضخامت خط | 1-10px | 2px | ضخامت خطوط اتصال |
| رنگ خط | RGB | #FFFFFF | رنگ خطوط (سفید) |

### کنترل‌های Zoom:

```
Zoom Level: 50% → 500%
  - 50%: دید کلی
  - 100%: اندازه واقعی
  - 200%+: جزئیات
```

---

## 📚 پاسخ به سوال شما:

### ❓ سوال: چطور حدود ساختارها (مولر، سانترال) را اضافه کنیم؟

### ✅ پاسخ:

از قابلیت **"Tracing ساختارها"** استفاده کنید!

#### راه‌حل کامل:

```
1️⃣ برای Molar (مولر):
   
   A) فک بالا:
      - فعال کنید: Tracing
      - نام: "Upper Molar 6"
      - کلیک‌های پیاپی دور مولر:
        • Mesial cusp
        • Buccal cusp
        • Distal cusp
        • Lingual cusp
        • بازگشت به mesial
      - ذخیره → outline کامل!
   
   B) فک پایین:
      - همین مراحل با نام "Lower Molar 6"

2️⃣ برای Central Incisor (سانترال):
   
   A) فک بالا:
      - نام: "Upper Central Incisor"
      - کلیک دور دندان:
        • Incisal edge
        • Distal surface
        • Cervical line
        • Mesial surface
        • بازگشت به incisal
      - ذخیره
   
   B) فک پایین:
      - همین مراحل با نام "Lower Central Incisor"

3️⃣ برای Root (ریشه):
   
   - نام: "Molar Root"
   - کلیک دور ریشه
   - ذخیره

4️⃣ برای Outline کلی:
   
   - نام: "Mandible Outline"
   - کلیک‌های پیاپی دور فک
   - ذخیره
```

#### مثال تصویری:

```
قبل از Tracing:
  • فقط نقاط landmarks دارید

بعد از Tracing:
  ┌────────────────┐
  │  ○ ← N        │  ← نقطه landmark
  │                │
  │   /‾‾‾\       │  ← Outline مولر (tracing)
  │  |     |       │
  │   \___/        │
  │                │
  │  ○ ← M6       │  ← نقطه landmark
  └────────────────┘
```

#### چندین ساختار همزمان:

```
می‌توانید چندین ساختار بکشید:
  ✅ Upper Molar 6 (بنفش)
  ✅ Lower Molar 6 (سبز)
  ✅ Upper Incisor (قرمز)
  ✅ Lower Incisor (آبی)
  ✅ Soft Tissue Profile (نارنجی)
```

---

## 🎓 Tips & Tricks:

### 💡 Tip 1: استفاده ترکیبی
```
نقاط + Tracing = تحلیل کامل!
  - نقاط landmarks برای محاسبات
  - Tracing برای visualization ساختارها
```

### 💡 Tip 2: ترتیب کار
```
1. ابتدا AI detection انجام دهید
2. نقاط نادرست را drag کنید
3. نقاط اضافی را اضافه کنید
4. outline ساختارها را بکشید
5. محاسبات را بررسی کنید
```

### 💡 Tip 3: Zoom برای دقت
```
برای tracing دقیق:
  1. Zoom به 200%+
  2. Pan به ناحیه مورد نظر
  3. کلیک‌های دقیق
  4. Zoom out برای دید کلی
```

### 💡 Tip 4: نام‌گذاری استاندارد
```
استاندارد پیشنهادی:
  - UM6 = Upper Molar 6
  - LM6 = Lower Molar 6
  - UC1 = Upper Central 1
  - LC1 = Lower Central 1
  - UL = Upper Lip
  - LL = Lower Lip
```

---

## ⚙️ تنظیمات پیشرفته (برای توسعه‌دهندگان):

### تغییر رنگ‌های پیش‌فرض:

```javascript
// در کد کامپوننت:
const DEFAULT_LANDMARK_COLORS = {
  S: '#2196F3',    // آبی
  N: '#2196F3',
  U1: '#F44336',   // قرمز
  L1: '#F44336',
  // ... می‌توانید تغییر دهید
};
```

### افزودن خطوط جدید:

```javascript
const DEFAULT_LINES = [
  { start: 'S', end: 'N', label: 'S-N' },
  { start: 'UM6', end: 'LM6', label: 'Molar Relationship' }, // جدید!
  // ...
];
```

---

## 🐛 عیب‌یابی:

### مشکل: نقاط drag نمی‌شوند
```
راه حل:
  ✅ مطمئن شوید روی نقطه کلیک می‌کنید (نه فضای خالی)
  ✅ دقت کنید که در حالت Pan نباشید
```

### مشکل: Tracing نمی‌شود
```
راه حل:
  ✅ سوئیچ "Tracing ساختارها" را فعال کنید
  ✅ نام ساختار را وارد کنید
  ✅ روی تصویر کلیک کنید (نه بیرون از canvas)
```

### مشکل: زاویه‌ها محاسبه نمی‌شوند
```
راه حل:
  ✅ مطمئن شوید نقاط مورد نیاز موجود هستند
     - SNA: نیاز به S, N, A
     - SNB: نیاز به S, N, B
     - و...
```

---

## 📖 خلاصه:

```
✅ تمام قابلیت‌های درخواستی اضافه شد!
✅ Drag & Drop نقاط با افکت hover
✅ تنظیمات کامل رنگ و ضخامت
✅ محاسبه زاویه‌های cephalometric
✅ افزودن و حذف نقاط
✅ Tracing ساختارها (مولر، سانترال، و...)
✅ UI حرفه‌ای و کاربرپسند
```

---

## 🚀 شروع کنید!

```
1. برو به: /dashboard/ai-model-test
2. عکس سفالومتری آپلود کن
3. مدل HRNet-W32 را انتخاب کن
4. شروع تست
5. نتایج با visualizer پیشرفته نمایش داده می‌شود!
6. از تمام قابلیت‌ها استفاده کنید! 🎉
```

---

**تبریک! شما الان یک سیستم کامل و پیشرفته برای آنالیز Cephalometric دارید!** 🎊

